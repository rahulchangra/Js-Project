// primitives  datatypes - string, number, boolean, null, undefined, symbol
// they are call by value
// they are immutable

const score = 100
const score1 = 100.5

const isLoggedIn = true
const outsideTemperature = null
let userEmail; // undefined

const id = Symbol("123") // unique identifier
const id1 = Symbol("123") // unique identifier
console.log(id === id1) // false


//javascript is dynamically typed language
// we can change the type of variable at any time during the execution of the program

// non primitive datatypes - object, array, function, date, regex
// they are call by reference
// they are mutable


